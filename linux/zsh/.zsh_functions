perm() {
	if [ ! -f $1 ] && [ ! -d $1 ]; then
		echo "perm: cannot access '$1': No such file or directory"
	else
		PERM=$(ls -ld $1 | sed 's| .*||g')
		echo $(unix-permissions convert.octal $PERM) \
			$(unix-permissions convert.stat $PERM) \
			$(unix-permissions convert.symbolic $PERM)
	fi
}
