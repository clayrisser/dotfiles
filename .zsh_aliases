#!/bin/bash

alias em="emacs -nw"
alias enpass="bash /opt/enpass/runenpass.sh"
alias sen="docker run -v /var/run/docker.sock:/run/docker.sock -ti -e TERM tomastomecek/sen"
alias venv="source ./env/bin/activate"
